import React from 'react';

export default class FormPage extends React.Component {
    render(): React.ReactNode {
        const title = 'Form';
        return (
            <div>
                <h1>{title}</h1>
            </div>
        );
    }
}
